import 'package:flutter/material.dart';

class IceSector extends StatelessWidget {
  const IceSector({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
